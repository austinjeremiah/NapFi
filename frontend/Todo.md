Real balance → needs Person 1's vault contract + Zama relayer SDK
Real receipts → needs Person 2's GET /api/receipts/:agentId
Real agent setup → needs Person 2's POST /api/setup
Vault withdraw → needs Person 1's contract address
Every one of those has a TODO comment already in place — one line swap when they're ready.