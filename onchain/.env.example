# Copy to `.env` (gitignored). Used for Sepolia deploys when set.
# DEPLOYER_PRIVATE_KEY=0x...64_hex_chars...

# Optional: public RPC (avoids Infura if unset)
# SEPOLIA_RPC_URL=https://ethereum-sepolia.publicnode.com
